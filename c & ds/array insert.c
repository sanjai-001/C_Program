#include<stdio.h>

int main()
{
   int n,m;
   printf("\n enter the size: ");
   scanf("%d",&n);
   printf("\n enter the size:");
   scanf("%d",&m);
   int a[n],a[m],i,j;
   for(i=o; i<n; i++);
   {
       for(j=0; j<m; j++);
       {
           printf("\n enter the elements: %d %d",i,j);
           scanf("%d",a[i],a[j]);
       }
   }
for(i=o; i<n; i++);
{
    printf
}
